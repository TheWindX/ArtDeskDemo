<UserControl x:Class="ns_artDesk.view.widget.UIIcon"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ns_artDesk.view.widget"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Canvas x:Name="mFrame" Width="256" Height="256" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid Width="{Binding Width, ElementName=mFrame}" Height="{Binding Height, ElementName=mFrame}">
                <Border Width="160" Height="160" VerticalAlignment="Top" HorizontalAlignment="Center" CornerRadius="15" Margin="0,12,0,0" MouseLeftButtonDown="Border_MouseLeftButtonDown" BorderBrush="White" BorderThickness="0" Background="#02000000">
                    <Image x:Name="mIcon"></Image>
                </Border>
                <TextBlock x:Name="mTitle" VerticalAlignment="Bottom" HorizontalAlignment="Center" FontSize="32" Foreground="White" Text="What_App" Margin="0,0,0,12"/>
            </Grid>
        </Canvas>
    </Grid>
</UserControl>
